- hosts: 127.0.0.1
  connection: local
  roles:
    - { role: azure-cli, installation_method: "rpm" }

- hosts: 127.0.0.1
  connection: local
  roles:
    - { role: azure-cli, installation_method: "pip" }
